% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/norm-band.R
\name{normativeZScore}
\alias{normativeZScore}
\title{Normative z-score of an observation}
\usage{
normativeZScore(value, model, age = NULL, sex = NULL, task = NULL)
}
\arguments{
\item{value}{Numeric observation. A scalar, a vector of scalars (scored
against a scalar model), or a waveform matching a waveform model's length.}

\item{model}{A \code{\link{NormativeModel}}.}

\item{age, sex, task}{Optional strata forwarded to the model to select the
applicable norm.}
}
\value{
Numeric z-score(s), matching the length of \code{value} (or the
waveform length).
}
\description{
Standardizes an observed value (or waveform) against a
\code{\link{NormativeModel}}: \eqn{z = (value - mean) / sd}, element-wise. A
scalar model recycles across a vector of observations; a waveform model
returns a per-timepoint z of the same length as the observation.
}
\examples{
m <- NormativeModel(mean = 1.30, sd = 0.15)
normativeZScore(c(1.00, 1.30, 1.60), m)
}
\seealso{
\code{\link[=NormativeModel]{NormativeModel()}}, \code{\link[=plotNormativeBand]{plotNormativeBand()}}
}
