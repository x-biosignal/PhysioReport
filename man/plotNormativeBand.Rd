% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/norm-band.R
\name{plotNormativeBand}
\alias{plotNormativeBand}
\title{Plot an observation against a normative band}
\usage{
plotNormativeBand(
  observed = NULL,
  model,
  bands = c(1, 2),
  time_axis = NULL,
  annotate_z = TRUE,
  age = NULL,
  sex = NULL,
  task = NULL,
  ...
)
}
\arguments{
\item{observed}{Numeric observation (a waveform, or a scalar broadcast across
the corridor). \code{NULL} draws just the corridor (no observed line and no
z annotation), so callers can overlay their own traces (e.g. left/right);
for a scalar model this requires \code{time_axis} to set the x extent.}

\item{model}{A \code{\link{NormativeModel}}.}

\item{bands}{Numeric vector of SD multiples to shade (default
\code{c(1, 2)}); one \code{geom_ribbon} layer per band, widest drawn first.}

\item{time_axis}{Optional numeric x-axis; defaults to the model's \code{time}
or the sample index.}

\item{annotate_z}{Logical; if \code{TRUE} (default) annotate the (max
absolute) z-score.}

\item{age, sex, task}{Optional strata forwarded to the model.}

\item{...}{Currently unused (reserved for future styling arguments).}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a normative corridor as graded \eqn{\pm1}SD and \eqn{\pm2}SD ribbons
around the reference median, overlays the observed trajectory, and annotates
the z-score. Uses the ecosystem colorblind-safe palette and report theme.
}
\examples{
\donttest{
wf <- NormativeModel(mean = sin(seq(0, pi, length.out = 101)),
                     sd = rep(0.1, 101), time = 0:100)
obs <- sin(seq(0, pi, length.out = 101)) + rnorm(101, 0, 0.05)
plotNormativeBand(obs, wf)
}
}
\seealso{
\code{\link[=NormativeModel]{NormativeModel()}}, \code{\link[=normativeZScore]{normativeZScore()}}
}
